import React from 'react'

export default function Todo() {
  return (
    <div>
        <ul>
            <li>Make it so the user can edit the entry by clicking on it and editing it on the page.</li>   
        </ul>
    </div>
  )
}
